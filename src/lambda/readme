
Lambda表达式：
    函数式接口：所谓SAM接口即Single Abstract Method,即该接口中只有一个抽象方法需要实现，当然该接口可以包含其他非抽象方法
    函数式接口是Lambda表达式和方法引用的目标类型
主要分为四大类：
    1）消费型接口：有参无返回值
    2）供给型接口：无参有返回值
    3）判断型接口：它的抽象方法：boolean test（有参） 无论给什么类型，都是用来判断条件，结果是由true、false
    4)功能型接口：它的抽象方法：有参有返回值
    5）无参无返回值不常用
语法：
    1）当形参列表是空参时，那么()是不能省略的
    2）当形参列表是非空参时，并且类型是确定的或者可以推断的，

